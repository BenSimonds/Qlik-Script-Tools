<block>
<name>Subbify_Template_Start</name>
<description>tab belonging to block: Subbify</description>
<type>TAB</type>
<text>
///$tab @0
SUB @0

LET vTable = '@1'; //Main Table Associated with Sub
LET vCounter = $(vCounter)+1;
TRACE $(vCounter) - Running tab @0 of document $(vDocumentName) with vSmartVar $(vSmartVar);
LET vExtractCurrentStart = num(Now());

/*
Notes:
*/
</text>
<replacelist>
	<replacelistitem id="@0">Name of sub being run, take from the tab name.</replacelistitem>
	<replacelistitem id="@1">Name of table to gather metadata about, should be the main table for the tab.</replacelistitem>
</replacelist>
</block>